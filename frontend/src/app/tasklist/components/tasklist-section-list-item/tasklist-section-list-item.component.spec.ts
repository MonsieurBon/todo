import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TasklistSectionListItemComponent } from './tasklist-section-list-item.component';

describe('TasklistSectionListItemComponent', () => {
  // let component: TasklistSectionListItemComponent;
  // let fixture: ComponentFixture<TasklistSectionListItemComponent>;

  beforeEach(async(() => {
    // TestBed.configureTestingModule({
    //   declarations: [ TasklistSectionListItemComponent ]
    // })
    // .compileComponents();
  }));

  beforeEach(() => {
    // fixture = TestBed.createComponent(TasklistSectionListItemComponent);
    // component = fixture.componentInstance;
    // fixture.detectChanges();
  });

  it('should create', () => {
    // expect(component).toBeTruthy();
  });
});
